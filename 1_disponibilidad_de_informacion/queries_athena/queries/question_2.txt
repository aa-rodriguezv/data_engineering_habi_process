CREATE OR REPLACE VIEW QUERY_2_TOP_SELLING_INFO_AVAILABLE AS 
SELECT ID
    , VALOR_VENTA
FROM "angel_test"."goods_data_cleaned_vw"
WHERE 
    NUM_BANOS >= 0
    AND AREA_LOTE >= 0
    AND ANTIGUEDAD >= 0
ORDER BY 2 DESC
LIMIT 10
;